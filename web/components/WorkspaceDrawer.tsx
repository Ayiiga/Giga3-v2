"use client";

/** Requested-path shim: the workspace drawer is ChatSidebar (single source of truth). */
export { ChatSidebar as WorkspaceDrawer } from "@/components/chat/ChatSidebar";
export type { ConversationItem } from "@/components/chat/ChatSidebar";
