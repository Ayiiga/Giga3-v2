/**
 * @deprecated Use BlogPostGrid for public listings (SSR-safe article links).
 * Kept as a thin re-export so stale imports do not pull Convex into blog HTML.
 */
export { BlogPostGrid as BlogGrid } from "@/components/blog/BlogPostGrid";
